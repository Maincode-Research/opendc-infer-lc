% Cross-vendor single-node comparison (235B-A22B): AMD MI355X vs NVIDIA B200.
% Requires: \usepackage{booktabs}. Peak SLO-qualified goodput (tok/s) at C*.
% AMD rows from Table~\ref{tab:main}; B200 rows from the per-point matrix
% (App.~\ref{tab:full-matrix-b200}). "---" = no SLO-qualified point.
\begin{table}[t]
  \centering
  \small
  \begin{tabular}{@{}l l r r r r@{}}
    \toprule
    \textbf{Vendor} & \textbf{Backend} & \textbf{LC-8K} & \textbf{LC-32K} &
    \textbf{LC-128K} & \textbf{LC-Cache} \\
    \midrule
    \multirow{2}{*}{\textbf{AMD MI355X}}
      & vLLM        & 22.2 & 3.6 & ---  & 47.5 \\
      & SGLang      & 31.3 & 6.5 & 0.9  & \textbf{99.6} \\
    \midrule
    \multirow{3}{*}{\textbf{NVIDIA B200}}
      & vLLM        & 36.4 & 7.2 & 1.2 & 64.6 \\
      & SGLang      & \textbf{38.3} & \textbf{9.1} & \textbf{1.3} & 63.8 \\
      & TRT-LLM     & 32.2 & 6.7 & 1.2 & \textbf{129.9} \\
    \bottomrule
  \end{tabular}
  \caption{\textbf{Cross-vendor single-node comparison}, Qwen3-235B-A22B, 8 GPUs,
  TP$=$8, BF16, 132K context. Each value is peak SLO-qualified output-token
  goodput (tok/s) at the highest qualifying concurrency C$^\star$. AMD rows are
  from Table~\ref{tab:main}; B200 rows are reduced from the per-point matrix in
  App.~\ref{tab:full-matrix-b200} (TensorRT-LLM is CUDA-only, hence B200-only).
  Two cross-vendor effects stand out. (i)~\textbf{B200 lifts the short/medium and
  the 128K workloads}: vLLM LC-8K rises $22.2\!\to\!36.4$ and LC-32K
  $3.6\!\to\!7.2$, and crucially B200 \emph{qualifies} LC-128K on every backend
  ($\approx1.2$) where AMD vLLM had no qualifying point---B200's faster 128K
  prefill brings TTFT under the 10\,s heavy bound. (ii)~\textbf{On the cached RAG
  workload the ranking flips by backend}: AMD SGLang ($99.6$) beats both B200
  vLLM and B200 SGLang ($\approx64$), but \textbf{B200 TensorRT-LLM tops the table
  at $129.9$}---so the LC-Cache winner is a CUDA-only stack, while on the
  AMD-available backends the MI355X is competitive-to-better. All runs had 100\%
  success and 100\% needle-recall quality.}
  \label{tab:b200}
\end{table}
