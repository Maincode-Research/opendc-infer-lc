"""Packaged as `opendc_data.configs` so `opendc-data build --config datasets.yaml`
resolves the shipped workload definitions after a plain `pip install`.
This file exists only for packaging; the YAML next to it is the real content."""
